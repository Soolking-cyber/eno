'use client'

/**
 * MARKETPLACE-EDITION STUB for trip-cards.tsx. See visa-cards.stub.tsx for the full reasoning — the
 * same alias, the same parity test, the same rule that TypeScript never sees this file so the shapes
 * are matched by hand.
 *
 * eno.vn is a licensed sàn TMĐT and offers no itinerary service, so none of these render. The
 * exports exist only so the shared chat page resolves its imports.
 */

import type { ComponentType } from 'react'

const Nothing: ComponentType<Record<string, unknown>> = () => null

export const TripAssistChips = Nothing
export const TripQuoteCard = Nothing
export const TripStatusCard = Nothing
export const TripWizardCard = Nothing
export const TripWizardLauncher = Nothing
