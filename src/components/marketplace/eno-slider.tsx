'use client'

/** Moved to the primitive layer: src/components/ui/slider.tsx (the single slider
 *  in the app — don't re-roll a raw <input type="range">). This file is a thin
 *  re-export kept so existing call sites keep working. */
export { Slider, EnoSlider } from '@/components/ui/slider'
