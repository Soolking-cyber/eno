# Cloud Build — eno.vn marketplace: build → push → deploy to Cloud Run.
# Runs from the GitHub trigger on main (also manually:
#   gcloud builds submit --config cloudbuild.yaml --substitutions=_TAG=$(git rev-parse --short HEAD))
# Build-time env (NEXT_PUBLIC_* inlining + SSG data collection) is pulled from
# Secret Manager into a BuildKit secret mount — never a layer, never metadata.
steps:
  # Pull the deploy image WHILE the docker build runs. Measured: the cloud-sdk:slim
  # pull is ~40s of the ~62s deploy step, whereas cloud-builders/docker is already
  # cached on the worker (0.009s). Cloud Build bills wall-clock, so overlapping it
  # with the ~300s build makes it free and gets fixes to prod ~40s sooner.
  # `|| true` is load-bearing: this step runs under set -e, and a transient GCR blip
  # must never be able to stop a production deploy.
  - id: warm-deploy-image
    name: gcr.io/cloud-builders/docker
    waitFor: ['-']
    entrypoint: bash
    args: ['-c', 'docker pull gcr.io/google.com/cloudsdktool/cloud-sdk:slim || true']
  - id: build-push
    name: gcr.io/cloud-builders/docker
    waitFor: ['-']
    entrypoint: bash
    secretEnv: ['BUILDENV']
    args:
      - -c
      - |
        set -euo pipefail
        printf '%s' "$$BUILDENV" > /workspace/buildenv.env
        # ⚠️ APPENDED, NOT ASSUMED — the same rule cloudbuild.services.yaml has always followed, and
        # this pipeline was the one missing it (fixed 2026-08-05). The asymmetry was the danger: the
        # SERVICES build guaranteed its edition in the pipeline, while the MARKETPLACE build — the
        # licensed company's — trusted the eno-root-env secret to contain the line. And the failure
        # direction is the illegal one: src/lib/edition.ts folds an ABSENT value to 'services', and
        # next.config.ts rejects only a value that is present-and-misspelled, so a dropped line in
        # the secret would silently build the SERVICES edition — visa, itinerary and PayPal — onto
        # eno.vn, with no gate firing anywhere. Appending here means this file wins regardless of
        # what the secret holds (the Dockerfile sources it with `set -a`, last assignment winning),
        # and next.config.ts still cross-checks it against NEXT_PUBLIC_APP_URL, so a secret pointing
        # at the wrong host fails the build rather than shipping mislabelled.
        printf '\nNEXT_PUBLIC_ENO_EDITION=marketplace\n' >> /workspace/buildenv.env
        # ⛔ BUILD-TIME, AND THERE IS NOWHERE ELSE IT CAN GO. MARKETPLACE_HOSTS_SERVICES is read by
        # next.config.ts, which runs inside `docker build` — so it has to be in THIS file (or in the
        # secret it appends to), and a `gcloud run services update --set-env-vars` would be a silent
        # no-op: the routes are decided at compile time by `pageExtensions`, not at request time.
        # It admits eno.vn to the partner-run visa CHAT (VietKite's `.svc.` routes and cards). It
        # never admits payments or eno's own e-visa marketing — those carry the stricter
        # `.forum.svc.` infix that no marketplace build lists, whatever this says.
        # ⚠️ ARMED 2026-08-14, AND THE ORDER IT WAITED FOR IS DONE: eno-root-env v22 repointed
        # VISA_SHOP_OWNER_EMAIL to info@vietkite.com.vn and set HIDDEN_DESK_OWNER_EMAILS to
        # support@eno.forum in ONE version, and all 14 VietKite listings were verified still present
        # in the feed, their storefront and their PDPs afterwards. Turning this on before that would
        # have painted eno's own e-visa conversations onto the licensed marketplace; splitting that
        # write would have deleted VietKite from browse, search, sitemap and the feeds with nothing
        # thrown.
        # ⚠️ THIS COMPILES THE PARTNER'S VISA CHAT ONTO eno.vn — the `.svc.` routes and cards. It
        # does NOT compile payments or eno's own e-visa marketing: those carry `.forum.svc.`, which
        # no marketplace build lists whatever this says.
        # ⚠️ VISA_THREADS_ENABLED IS STILL ABSENT AND MUST STAY ABSENT UNTIL THIS REVISION SERVES.
        # It is the runtime switch the PDP gates on; set it against a build without this flag and
        # `isVisaProduct` goes true while VisaStart is still the stub, so the Chat button renders as
        # nothing on 14 live listings.
        # ⛔ AND THE SAME TRAP RUNS BACKWARDS — A ROLLBACK IS NOT SAFE ON ITS OWN (reviewer, 2026-08-14).
        # The real invariant is not "until this revision serves" but "while ANY serving revision lacks
        # this flag". Rolling Cloud Run back to a pre-flag revision, or reverting this line, while
        # VISA_THREADS_ENABLED is set in the secret puts a stub build under a live runtime flag and
        # reproduces exactly the broken PDPs above. UNSET THE RUNTIME FLAG FIRST, THEN ROLL BACK.
        # ⚠️ LEGACY FORUM THREADS WERE CHECKED, NOT ASSUMED. 5 conversations carrying 147 visa cards
        # exist in the shared database, ALL owned by the Eno desk (support@eno.forum) — measured
        # 2026-08-14. Compiling the cards in does not surface them on eno.vn, and two independent
        # things stop it: deskSellerIds() still resolves that desk from HIDDEN_DESK_OWNER_EMAILS, so
        # the inbox and thread-open exclude them (api/conversations/route.ts, [id]/route.ts:160); and
        # their anchor listings are Eno's, not VietKite's, so threadKind() answers 'listing' even if
        # one were reached. Belt and braces, and neither is this flag.
        printf '\nMARKETPLACE_HOSTS_SERVICES=true\n' >> /workspace/buildenv.env
        export DOCKER_BUILDKIT=1
        docker build \
          --secret id=buildenv,src=/workspace/buildenv.env \
          -t asia-southeast1-docker.pkg.dev/$PROJECT_ID/eno/eno-vn:${_TAG} \
          -t asia-southeast1-docker.pkg.dev/$PROJECT_ID/eno/eno-vn:latest \
          .
        rm -f /workspace/buildenv.env
        docker push --all-tags asia-southeast1-docker.pkg.dev/$PROJECT_ID/eno/eno-vn
  - id: deploy
    name: gcr.io/google.com/cloudsdktool/cloud-sdk:slim
    waitFor: ['build-push', 'warm-deploy-image']
    entrypoint: gcloud
    args:
      - run
      - deploy
      - eno-vn
      - --image=asia-southeast1-docker.pkg.dev/$PROJECT_ID/eno/eno-vn:${_TAG}
      - --region=asia-southeast1
      - --project=$PROJECT_ID
      # ⚠️ DECLARED HERE, NOT LEFT TO WHATEVER THE SERVICE HAPPENS TO HOLD — this flag is load-bearing
      # and its absence is invisible.
      #
      # Next's `after()` runs its callback AFTER the response is flushed, and this app leans on that
      # in 53 places: Vertex AI Search indexing (src/lib/listing-index.ts), view + contact tracking,
      # review aggregation, translation caching, notification reads, video transcoding, dispute and
      # admin follow-ups. Under Cloud Run's DEFAULT cpu-throttling=true the instance's CPU is cut to
      # ~zero the moment the response completes, so that work is frozen and then discarded when the
      # instance is reclaimed. It does not fail loudly — it cannot even log, because logging needs
      # the CPU it just lost — so every one of those features degrades in silence.
      #
      # Found 2026-08-01 by diagnosing "the vertex indexer doesn't work": eno-vn was running
      # cpu-throttling=true while eno-forum ran with it disabled, because cloudbuild.services.yaml
      # declares --no-cpu-throttling for eno-forum and this file declared nothing for eno-vn. The
      # sibling deploy carried the setting and this one silently did not — the same
      # config-applied-to-one-service-and-not-the-other shape as the DATABASE_URL and x-eno-edge
      # cutover failures.
      #
      # NOT --min-instances: eno-forum pins one instance, but that bills continuously and `after()`
      # only needs CPU for the seconds between the response and the callback finishing, which an
      # instance that is alive-but-idle already has. Cold starts are a latency question, not this one.
      - --no-cpu-throttling
      # ⚠️ DECLARED 2026-08-02 BECAUSE eno.vn WAS OOM-ING AND NOBODY KNEW.
      #
      # This file never declared resources at all, so the service kept whatever an earlier manual
      # setup left it: 1 vCPU / 1 GiB. Cloud Run logged "Memory limit of 1024 MiB exceeded" twice
      # in 30 days (1026 and 1043 MiB used) — the container is killed and restarts, which on the
      # licensed marketplace means dropped in-flight requests and, since `after()` callbacks die
      # with the instance, silently discarded background work of exactly the kind the
      # --no-cpu-throttling line above exists to protect.
      #
      # 2Gi matches eno-forum, which runs the SAME IMAGE and has logged zero OOMs on it. Memory is
      # the cheap axis on Cloud Run — roughly a tenth of CPU per unit — so this buys reliability
      # for very little, and the same commit takes a whole always-on 2-vCPU instance off
      # eno-forum, which is where the money actually was.
      #
      # CPU deliberately stays at 1: eno.vn's cost is bounded by scaling to zero when idle
      # (no --min-instances here, on purpose), and 1 vCPU has never been the constraint — memory
      # was.
      - --memory=2Gi
      - --cpu=1
      # ⚠️ SCALES TO ZERO, DELIBERATELY — COST BEATS COLD STARTS HERE (owner, 2026-08-02:
      # "leave coldstart i dont need cranked up bills").
      #
      # The trade-off is measured, not assumed. eno-vn cold-started 19 times in 24 hours and the
      # slowest requests in that window were 10.3–11.2 SECONDS, which is what made Google's brand
      # review report "Your home page URL https://eno.vn is unresponsive". A warm instance would
      # remove that, and it costs a 24/7 CPU+memory charge under --no-cpu-throttling above.
      #
      # The owner's call is that an occasional slow first request is cheaper than an always-on
      # instance on a pre-launch site, and the audit complaint it caused has since cleared anyway.
      # ⚠️ EXPLICIT 0, not an omitted flag: `gcloud run deploy` applies only what you pass, so
      # deleting this line would leave whatever the service currently holds — which is exactly how
      # the eno-forum pin survived a deploy that was meant to remove it.
      - --min-instances=0
      - --quiet
availableSecrets:
  secretManager:
    - versionName: projects/$PROJECT_ID/secrets/eno-root-env/versions/latest
      env: BUILDENV
substitutions:
  _TAG: manual
options:
  # No machineType pin: the project lacks E2_HIGHCPU_8 quota in the trigger
  # region (europe-west1) — default machines build this in acceptable time.
  # Measured 2026-07-25, so nobody re-litigates it: only ~104s of a ~362s build is
  # non-CPU-bound, so an upgrade must cost <= $0.0105/min to break even. E2_HIGHCPU_8
  # is $0.016/min — it loses even with an infinitely fast CPU — and pinning any
  # machineType also forfeits the 2,500 free e2-medium build-minutes (~$7.50/mo).
  logging: CLOUD_LOGGING_ONLY
  dynamicSubstitutions: true
timeout: 1800s
