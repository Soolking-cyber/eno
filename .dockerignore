# Keep the build context small + secrets out of the image.
.git
.next
node_modules
# The forum is a SEPARATE image (apps/forum/Dockerfile, its own context).
apps
# Native shells + test artifacts never belong in a server image.
ios
e2e
test-results
playwright-report
npm-debug.log*
dev.log
server.log
.env
.env.*
.vercel
*.md
.claude
scratchpad
coverage
.DS_Store
